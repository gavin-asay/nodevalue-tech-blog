<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>nodeValue Web Dev Blog</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>nodeValue Web Dev Blog</h1>
            <nav>
                {{#if loggedIn}}
                <a href="/dashboard">dashboard</a>
                <button id="logout" class="btn-no-style">logout</button>
                {{else}}
                <a href='/login'>login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>

        <footer>
            © {{year}} Gavin Asay
        </footer>

    </div>
</body>

</html>